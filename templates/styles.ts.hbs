import styled, { css } from 'styled-components'
import { {{pascalCase name}}Props } from '.'

export const Wrapper = styled.main<{{pascalCase name}}Props>`
  ${({ theme }) => css`
    color: ${theme.colors.mainTxt};
  `}
`
